# =============================================================================
# EcodiaOS Frontend — Environment Variables
# =============================================================================
# Copy to .env and adjust as needed.

# API base URL
VITE_API_URL=/api

# WebSocket URL (auto-detected from window.location if not set)
VITE_WS_URL=
